---
title: "WTA Rankings"
description: "Notes on the model."
date: 2026-09-06
date-modified: 2026-09-09
status: current
toc: false
page-layout: full
---

:::: model-field-sheet
## Purpose

This model estimates WTA player strength over time and across surfaces, with uncertainty, by treating historical market probabilities as noisy observations of latent player strength. The aim of this model is not to beat today's market, but to use public pricing history to create an accurate, holistic ranking of tour players available anytime. Current estimates are published on the [WTA Rankings](/tennis/rankings/) page.

The system has two stages which share the same generative model. Each off-season, the global model learns the shared parameters while integrating over the player states. During the season, the global parameters are fixed while the online model estimates latent player strengths before each day's matches.

## Data

One row per match: players $i$ and $j$, date $t$, surface $s$, player ages, prior WTA appearances, surface type, and decimal odds $o_{i,m}$ and $o_{j,m}$. These data were acquired for nearly every WTA match from 2019 to 2026.

The odds are converted to raw implied probabilities and de-vigged:

$$
\pi_{i,m}=\frac{1}{o_{i,m}},
\qquad
p_{i,m}=\frac{\pi_{i,m}}{\pi_{i,m}+\pi_{j,m}},
\qquad
y_m=\log\frac{p_{i,m}}{1-p_{i,m}}.
$$

The raw bookmaker margin is $v_m=\pi_{i,m}+\pi_{j,m}-1$. The model uses its training-standardized value,

$$
V_m=\frac{v_m-\bar v_{\mathrm{train}}}{s_{v,\mathrm{train}}}.
$$

The stored odds are the source's first available bookmaker; though bookmaker identity is not preserved, nearly the entirety of these data are sourced from *bet365*. When forecasting a match, its own price is not used: all matches on a date are predicted before prices from that date update the player states. Match outcomes are used only for evaluation.

## Parameters

**Player states**

- $x_{i,t}$ = player $i$'s overall strength at time $t$
- $\mathbf u_i=(u_{i,\mathrm{hard}},u_{i,\mathrm{clay}},u_{i,\mathrm{grass}})$ = player $i$'s surface effects
- $z$ = all player states and surface effects

**Shared parameters**

$\operatorname{Normal}(\mu,\sigma)$ denotes a distribution with mean $\mu$ and standard deviation $\sigma$; $\operatorname{Normal}_2(\boldsymbol\mu,\boldsymbol\Sigma)$ uses covariance matrix $\boldsymbol\Sigma$. $\operatorname{LogNormal}(m,s)$ has median $m$ and log-scale standard deviation $s$.

- $\theta$ = all shared system parameters
- $\tau_0$ = population entry SD, in log-odds; $\tau_0\sim\operatorname{LogNormal}(1.5,0.75)$
- $\sqrt q$ = process SD, in log-odds per square-root year; $\sqrt q\sim\operatorname{LogNormal}(0.5,1)$
- $\sqrt r$ = base observation SD, in log-odds; $\sqrt r\sim\operatorname{LogNormal}(0.25,0.75)$
- $\tau_s$ = SD of the orthonormal surface contrasts, in log-odds; $\tau_s\sim\operatorname{LogNormal}(0.35,0.75)$
- $\beta_g$ = annual pre-peak growth, in log-odds; $\beta_g\sim\operatorname{LogNormal}(0.4,0.75)$
- $\beta_d$ = annual post-peak decline, in log-odds; $\beta_d\sim\operatorname{LogNormal}(0.15,1)$
- $\gamma_e$ = change in log entry SD per standardized unit of prior experience; $\gamma_e\sim\operatorname{Normal}(0,0.5)$
- $\gamma_v$ = change in log observation SD per standardized unit of bookmaker margin; $\gamma_v\sim\operatorname{Normal}(0,0.5)$

## Model map

```{mermaid}
flowchart TB
  experience["entry experience<br/>Eᵢ"] --> initial["entry strength<br/>xᵢ,1"]
  entry["entry parameters<br/>τ₀, γₑ"] --> initial

  initial --> history["previous strengths<br/>xᵢ,t−1, xⱼ,t−1"]
  history --> strength["current strengths<br/>xᵢ,t, xⱼ,t"]
  age["age and elapsed time<br/>Aᵢ,t, Δt"] --> strength
  drift["age and drift parameters<br/>β, q"] --> strength

  surface["surface parameter<br/>τₛ"] --> effects["surface effects<br/>uᵢ,s, uⱼ,s"]

  strength --> matchup["matchup mean<br/>ηₘ"]
  effects --> matchup

  margin["bookmaker margin<br/>Vₘ"] --> noise["observation SD<br/>√rₘ"]
  observation["observation parameters<br/>√r, γᵥ"] --> noise

  matchup --> price["market log-odds<br/>yₘ"]
  noise --> price
```

## Model

**Player evolution**

A player enters the model as a draw from the tour population:

$$
\log \tau_i=\log\tau_0+\gamma_e E_i,
\qquad
x_{i,1} \sim \operatorname{Normal}(0,\tau_i).
$$

$E_i$ is the training-standardized value of $\log(1+n_i)$, where $n_i$ is player $i$'s number of stored WTA appearances before entering the panel.

The live model fixes peak age at 24 and uses the piecewise-linear age path:

$$
a(A;\beta)=
\begin{cases}
\beta_g(A-24), & A<24,\\
-\beta_d(A-24), & A\geq24.
\end{cases}
$$

Peak age was fixed at 24 before evaluation. [Short-horizon refits](/assets/data/wta-peak-sensitivity.csv) could not distinguish peaks from 24 through 28: identifying the aging curve requires forecasts over years rather than matches.

Player strength then evolves as:

$$
x_{i,t} \mid x_{i,t-1}
\sim
\operatorname{Normal}\!\left(
x_{i,t-1}+a(A_{i,t};\beta)-a(A_{i,t-1};\beta),
\sqrt{q\Delta t}
\right).
$$

$\Delta t$ is elapsed calendar time in years. The mean follows the age path; variance grows linearly with elapsed time.

Each player has zero-sum random effects for hard, clay and grass. They are represented using two orthonormal Helmert contrasts.

$$
\mathbf c_i\sim\operatorname{Normal}_2(\mathbf 0,\tau_s^2\mathbf I_2),
\qquad
\mathbf u_i=\mathbf C\mathbf c_i,
$$

$$
\mathbf C=
\begin{pmatrix}
1/\sqrt 2 & 1/\sqrt 6\\
-1/\sqrt 2 & 1/\sqrt 6\\
0 & -2/\sqrt 6
\end{pmatrix},
\qquad
\mathbf 1^\mathsf T\mathbf u_i=0.
$$

This is a convenient way to impose $u_{i,\mathrm{hard}}+u_{i,\mathrm{clay}}+u_{i,\mathrm{grass}}=0$ while giving every surface the same prior treatment. Each named surface effect has population SD $\sqrt{2/3}\,\tau_s$; effects for two different surfaces have correlation $-1/2$ because increasing one must be offset by the others.

**Match observations**

A match brings the two player states together. For match $m$, the expected market log-odds are the difference between their overall strengths and surface effects:

$$
\eta_m
=
x_{i,t}-x_{j,t}+u_{i,s}-u_{j,s},
$$

Observation SD changes with bookmaker margin:

$$
\log\sqrt{r_m}=\log\sqrt r+\gamma_v V_m,
\qquad
y_m \mid z,\theta
\sim
\operatorname{Normal}(\eta_m,\sqrt{r_m}).
$$

$r_m$ determines how strongly the observed price updates the player states.

**Off-season global fit**

Let $y_{\mathrm{train}}$ contain the eligible market log-odds from the preceding seasons in a ten-season window. The 2026 fit uses every available season from 2019 through 2025. The shared parameters are estimated by maximum a posteriori probability while the player states are integrated out:

$$
\hat\theta
=
\underset{\theta}{\arg\max}
\left\{
\log\int
p(y_{\mathrm{train}}\mid z,\theta)
p(z\mid\theta)\,dz
+\log p(\theta)
\right\}.
$$

The Gaussian integral is evaluated exactly in sparse precision form; player states are not optimizer coordinates.

**In-season online fit**

Given fitted parameters $\hat\theta$, estimate the player states from prices observed before date $t$:

$$
p(z_t\mid y_{<t},\hat\theta).
$$

Carry the state distributions forward, predict a given set of matches, then condition on the new prices. This is implemented as an information-form Kalman update.

Dynamic player states, filtering and offline parameter estimation are established parts of skill-rating models; [Duffield, Power and Rimella](https://academic.oup.com/jrsssc/article/73/5/1262/7734616) demonstrate that workflow on WTA results. Surface-specific tennis states also appear in [Gorgi, Koopman and Lit](https://doi.org/10.1111/rssa.12464), while [Ingram](https://doi.org/10.1515/jqas-2018-0008) separates serve and return skill using point outcomes. [Leitner, Zeileis and Hornik](https://doi.org/10.1016/j.ijforecast.2009.10.001) recover latent ability from bookmaker consensus. Here, de-vigged match prices are the observations, and the linear-Gaussian form allows the player states to be integrated exactly during the global fit.

## Results

The evaluation contains 6,381 matches from the 2024 and 2025 seasons. There are two benchmarks.

1.  The market benchmark uses the de-vigged price for the match being predicted.
2.  The Elo benchmark initializes every player at 1500, uses the conventional 400-point logistic scale with a fixed $K=32$, and warms up on matches from 2021 through 2023. It maintains one rating across surfaces and uses only outcomes observed before that day.

::: model-field-sheet
The fraction of market signal retained is defined against a coin flip forecast:

$$
S
=
\frac{\operatorname{LL}(0.5)-\operatorname{LL}(\text{model})}
{\operatorname{LL}(0.5)-\operatorname{LL}(\text{market})}.
$$
:::

```{=html}
<table class="model-results-table">
  <colgroup>
    <col class="surface-column">
    <col class="matches-column">
    <col class="model-column">
    <col class="model-column">
    <col class="model-column">
  </colgroup>
  <thead>
    <tr><th scope="col">Surface</th><th scope="col">N</th><th scope="col">Market</th><th scope="col">Player-state</th><th scope="col">Elo</th></tr>
  </thead>
  <tbody>
    <tr class="metric-row"><th colspan="5" scope="rowgroup">Log loss</th></tr>
    <tr class="overall-row"><th scope="row">Overall</th><td>6,381</td><td>0.581</td><td>0.597</td><td>0.628</td></tr>
    <tr><th scope="row">Hard</th><td>3,951</td><td>0.586</td><td>0.600</td><td>0.633</td></tr>
    <tr><th scope="row">Clay</th><td>1,663</td><td>0.561</td><td>0.579</td><td>0.617</td></tr>
    <tr><th scope="row">Grass</th><td>767</td><td>0.596</td><td>0.620</td><td>0.630</td></tr>
    <tr class="metric-row"><th colspan="5" scope="rowgroup">Brier score</th></tr>
    <tr class="overall-row"><th scope="row">Overall</th><td>6,381</td><td>0.199</td><td>0.206</td><td>0.220</td></tr>
    <tr><th scope="row">Hard</th><td>3,951</td><td>0.202</td><td>0.207</td><td>0.222</td></tr>
    <tr><th scope="row">Clay</th><td>1,663</td><td>0.191</td><td>0.199</td><td>0.215</td></tr>
    <tr><th scope="row">Grass</th><td>767</td><td>0.206</td><td>0.216</td><td>0.220</td></tr>
    <tr class="metric-row"><th colspan="5" scope="rowgroup">Signal retained</th></tr>
    <tr class="overall-row"><th scope="row">Overall</th><td>6,381</td><td>100.0%</td><td>85.4%</td><td>57.7%</td></tr>
    <tr><th scope="row">Hard</th><td>3,951</td><td>100.0%</td><td>86.8%</td><td>56.3%</td></tr>
    <tr><th scope="row">Clay</th><td>1,663</td><td>100.0%</td><td>86.0%</td><td>57.8%</td></tr>
    <tr><th scope="row">Grass</th><td>767</td><td>100.0%</td><td>76.1%</td><td>64.9%</td></tr>
  </tbody>
</table>
```

In a day-clustered bootstrap, the model's mean log loss was 0.0163 above the market [95% interval: 0.0117, 0.0207] and 0.0312 below Elo [0.0245, 0.0376].

## Interpretation

1.  The model retains much of the market’s signal (85.4%), particularly compared with standard Elo (57.7%), without using the predicted match’s price or outcome.
2.  The model’s primary advantage is that any matchup on any surface can be simulated.
3.  The model struggles on grass surfaces at predicting both match means and standard deviations. Interestingly, the model is more aggressive here, where it has the smallest sample and the lowest performance, than anywhere else.
4.  Surface effects have roughly 10% as much cross-player spread as surface-agnostic latent strength.
5.  Player-specific residuals are correlated over short timescales, motivating the first two items under Next steps.
6.  Match-to-match predictions are generally not separated enough in time to identify peak age.

The [ranking states](/assets/data/wta-rankings-latest.csv), [surface summary](https://github.com/spazznolo/spazznolo.github.io/blob/master/scripts/wta_surface_dispersion.py), and validation materials reproduce these results.

## Validation

**Prediction means**

Before each match, the model produces a win probability $\hat p_m$. Outcome calibration is checked by regressing the observed result $W_m$ on the model log-odds:

$$
W_m
\sim
\operatorname{Bernoulli}\!\left(
\operatorname{logit}^{-1}\!\left[
\alpha+\lambda\operatorname{logit}(\hat p_m)
\right]
\right).
$$

The intercept $\alpha$ tests whether predictions are systematically high or low. The slope $\lambda$ tests whether their spread is right: one is calibrated, above one means the model is too conservative, and below one means it is too extreme.

| Subset | Matches | Intercept \[95% interval\] | Slope \[95% interval\] |
|:----------------|----------------:|--------------------:|-----------------:|
| **Overall** | **6,381** | **−0.01 \[−0.07, 0.05\]** | **1.05 \[0.98, 1.12\]** |
| 2024 | 3,167 | −0.02 \[−0.10, 0.05\] | 1.08 \[0.99, 1.18\] |
| 2025 | 3,214 | +0.01 \[−0.07, 0.08\] | 1.01 \[0.91, 1.11\] |
| Hard | 3,951 | −0.02 \[−0.08, 0.05\] | 1.02 \[0.93, 1.11\] |
| Clay | 1,663 | +0.02 \[−0.08, 0.12\] | 1.18 \[1.05, 1.34\] |
| Grass | 767 | −0.02 \[−0.18, 0.14\] | 0.91 \[0.75, 1.07\] |

**Prediction standard deviations**

Before each match, the model produces a distribution for the market log-odds rather than a point estimate. Its predictive variance is the sum of uncertainty in the latent matchup and fitted observation variance:

$$
s_m^2=\mathbf h_m^{\mathsf T}\mathbf P_t\mathbf h_m+r_m.
$$

$\mathbf P_t$ is the covariance of the current player states, while $\mathbf h_m$ selects the two players and the surface for match $m$. If the predictive SD $s_m$ is calibrated, the standardized innovations

$$
e_m=\frac{y_m-\eta_m}{s_m}
$$

have SD one and the prediction intervals attain their stated coverage.

| Surface     | Innovation SD | 50% interval | 80% interval | 95% interval |
|:------------|--------------:|-------------:|-------------:|-------------:|
| Hard        |         0.953 |        53.0% |        83.4% |        96.3% |
| Clay        |         0.976 |        52.1% |        81.5% |        95.6% |
| Grass       |         1.121 |        48.1% |        77.4% |        91.9% |
| **Overall** |     **0.981** |    **52.2%** |    **82.2%** |    **95.6%** |

The pooled standardized innovations have mean +0.018. Overall, the predictive SD is close to calibrated. Hard and clay intervals are slightly too wide; grass intervals are too narrow.

Against later market information, standardized revisions had SD 0.991 for played-surface player states and 1.035 for matchup contrasts, with 95% coverage of 94.4% and 94.5%. The [state revisions](/assets/data/wta-state-revision-audit.csv), [matchup revisions](/assets/data/wta-matchup-revision-audit.csv) and [audit script](https://github.com/spazznolo/spazznolo.github.io/blob/master/scripts/wta_state_revision_audit.py) reproduce the check.

These diagnostics are reproducible using the [evaluation data](/assets/data/wta-model-validation.csv) and [validation script](https://github.com/spazznolo/spazznolo.github.io/blob/master/scripts/wta_validation.py).

## Next steps

1.  Split drift states into chronic and acute, to capture correlated residual on short-term windows.
2.  Add latest match result as a feature in model.
3.  Evaluate the aging curve using multi-year forecasts without intermediate market updates.
4.  Revisit clay and grass calibration as more seasons accumulate.
5.  Split player strength into serve and return states using point-level data, then measure what they add to match prediction.

::::
